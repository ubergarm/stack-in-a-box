http://localhost {
    redir https://localhost{uri}
}

https://localhost {
    root /html

    proxy /admin rdb:8080 {
        without /admin
    }

    log stdout
    errors stderr

    tls self_signed
}
